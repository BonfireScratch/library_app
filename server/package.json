{
  "name": "library-app-backend",
  "version": "1.0.0",
  "private": true,
  "description": "The back-end code for library-app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.7.1",
    "sequelize": "^6.6.5",
    "xlsx": "^0.17.0"
  }
}
